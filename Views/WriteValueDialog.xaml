<Window x:Class="OpcUaCommunicationEngine.Views.WriteValueDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Write Tag Value"
        Height="350" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Margin" Value="0,0,0,2"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tag Info Section -->
        <Border Grid.Row="0" BorderBrush="#CCC" BorderThickness="1" Background="White" CornerRadius="4" Padding="10" Margin="0,0,0,15">
            <StackPanel>
                <Label Content="Tag Name"/>
                <TextBox x:Name="TxtTagName" IsReadOnly="True" Background="#F0F0F0"/>

                <Label Content="Node ID"/>
                <TextBox x:Name="TxtNodeId" IsReadOnly="True" Background="#F0F0F0"/>

                <Label Content="Data Type"/>
                <TextBox x:Name="TxtDataType" IsReadOnly="True" Background="#F0F0F0"/>

                <Label Content="Current Value"/>
                <TextBox x:Name="TxtCurrentValue" IsReadOnly="True" Background="#F0F0F0"/>
            </StackPanel>
        </Border>

        <!-- Write Value Section -->
        <Border Grid.Row="1" BorderBrush="#CCC" BorderThickness="1" Background="White" CornerRadius="4" Padding="10" Margin="0,0,0,15">
            <StackPanel>
                <Label Content="New Value *"/>
                <TextBox x:Name="TxtNewValue" Height="60" TextWrapping="Wrap" AcceptsReturn="True"
                         VerticalContentAlignment="Top"/>

                <TextBlock x:Name="TxtHint" Text="" Foreground="Gray" FontStyle="Italic" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Write" Width="80" Padding="10,5" Margin="0,0,10,0" Click="WriteButton_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="80" Padding="10,5" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
